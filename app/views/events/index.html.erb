<p style="color: green"><%= notice %></p>

<div id="events">
  <div class="row row-cols-1 row-cols-md-3 g-4 mt-1">
    <% @events.each do |event| %>
      <%= render event %>
    <% end %>
  </div>
</div>